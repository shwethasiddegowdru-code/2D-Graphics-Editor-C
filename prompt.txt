1.Complete the TODO sections in the provided C program for a 2D Graphics Editor. Implement clearPicture(), displayPicture(), setPixel(), drawLine(), drawRectangle(), drawCircle(), and drawTriangle(). Use '*' for drawing and '_' for empty pixels. Keep the existing menu and structure unchanged.
2.Modify the drawLine() function in the 2D Graphics Editor to use Bresenham's Line Drawing Algorithm so that lines can be drawn correctly in all directions and slopes. Do not change the rest of the program.
3.Extend the 2D Graphics Editor in C to store multiple shapes using a structure. Create a shape structure that can represent lines, rectangles, circles, and triangles. Store created objects in an array and redraw them whenever the picture is displayed.
4.Add object management features to the 2D Graphics Editor. Implement options to add new objects, delete existing objects by index, and list all stored objects. Keep the program menu-driven and use structures for object storage.
5.Add object management features to the 2D Graphics Editor. Implement options to add new objects, delete existing objects by index, and list all stored objects. Keep the program menu-driven and use structures for object storage.
6.Refactor the 2D Graphics Editor code for better modularity. Create separate functions such as init_canvas(), render(), show_canvas(), print_header(), and print_menu(). Ensure all stored objects are rendered on an 80x24 canvas using ASCII graphics. Maintain support for adding, deleting, modifying, listing, and displaying shapes.
7.Develop a menu-driven 2D Graphics Editor in C using ASCII graphics on an 80x24 canvas. Support drawing lines, rectangles, circles, and triangles using '*' characters. Store shapes using structures in an array. Provide options to add, delete, modify, list, and display objects. Use Bresenham's algorithm for lines and midpoint circle drawing for circles. Organize the program using modular functions such as canvas initialization, rendering, menu display, and object management.
